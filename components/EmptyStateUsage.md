# EmptyState usage notes

Replace simple 'nothing here' messages with the canonical EmptyState component (if present in the repo) and provide context-specific CTAs.

TODO:
- Find pages with empty messages: pages/Wishlist, pages/Orders, seller dashboard empty states.
- Replace with components/EmptyState (or create it if missing) and wire CTAs.
